// Page Loader Configuration
// Change these settings to customize the page loader behavior

export const loaderConfig = {
  // Enable or disable the page loader
  enabled: true,
  
  // Duration in milliseconds (how long the loader shows)
  duration: 1500,
  
  // Loader variant: 'simple' | 'advanced'
  variant: 'simple',
  
  // Animation style (for advanced variant): 'fade' | 'slide' | 'scale' | 'curtain'
  animationStyle: 'fade',
  
  // Colors
  colors: {
    primary: '#59a4b5',
    secondary: '#4a9aaa',
    tertiary: '#3b8a9a',
  },
  
  // Logo text
  logo: {
    main: 'HOTEL',
    sub: 'BEACH',
  },
};
